# Kido Dev Agentic AI Backend — Python Dependencies
# Core
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-dotenv>=1.0.0

# Pydantic — pin to v2 latest (uses ABI3 forward compat for Python 3.14+)
pydantic>=2.9.0

# HTTP clients
httpx>=0.28.0
aiohttp>=3.10.0

# Supabase
supabase>=2.10.0

# Utilities
python-multipart>=0.0.20
